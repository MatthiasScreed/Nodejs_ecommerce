{
  "name": "nodejs_proj",
  "version": "1.0.0",
  "description": "discover_proj",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "chris",
  "license": "ISC",
  "dependencies": {
    "browser-refresh": "^1.7.3",
    "express": "^4.17.1",
    "pug": "^2.0.4"
  },
  "script": {
    "start": "browser-refresh index",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
